{%- render 'breadcrumb' -%}

{% schema %}
{
  "name": "t:sections.breadcrumb.name",
  "class": "section",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "default": "Breadcrumb",
      "label": "t:sections.breadcrumb.settings.title.label"
    }
  ],
  "presets": [
    {
      "name": "t:sections.breadcrumb.presets.name"
    }
  ]
}
{% endschema %}
