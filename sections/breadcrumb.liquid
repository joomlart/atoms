{% render 'breadcrumb' %}

{% schema %}
{
  "name": "t:sections.breadcrumb.name",
  "class": "section",
  "tag": "section",
  "presets": [{
    "name": "Call to Actions",
    "category": "Text"
  }]
}
{% endschema %}
